{% extends 'base.html.twig' %}

{% block title %}Bienvenue dans votre panier!{% endblock %}

{% block body %}
<h1>Mon panier</h1>

{% if panier %}

    {% for product in panier.elements %}
        <div>
            {{ product.produit.nom }} : de quantité {{ product.quantite }}
        </div>
    {% endfor %}

{% else %}

    <h1>Commander</h1>

{% endif %}

<a href="{{ path('panier_clear') }}">Nettoyer mon panier</a>
<a href="{{ path('payment') }}">Payer maintenant</a>

{% endblock %}